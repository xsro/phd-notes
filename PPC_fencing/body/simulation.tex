\section{Simulation Verification}
\label{sec:simulation}
\textbf{[TODO]} Suggested content:
\begin{itemize}
    \item Compare enclosure error $\dist(p,p_0)$ against the prescribed
    envelope $\rho(t)$ for both the asymptotic \eqref{eq:rho-exp} and
    prescribed-time \eqref{eq:rho-pt} performance functions.
    \item Show minimum pairwise distance stays $>d$ (fence preserved).
    \item Compare with the baseline fencing controller (no PPC) to
    highlight the improved, user-specifiable transient behavior.
\end{itemize}
