\section{Prescribed-Performance Fencing Controller Design}
\label{sec:design}
\textbf{[TODO]} Suggested roadmap:
\begin{itemize}
    \item Express the enclosure error $e=\dist(p,p_0)$ via the PPC
    transformation \eqref{eq:xi-dyn}: $e=\rho\tanh\xi$, so that keeping
    $\xi$ bounded enforces $e<\rho(t)$ (P4).
    \item Use backstepping on the double-integrator centroid error
    $\tilde p$: its dynamics \eqref{eq:centroid-dyn} is second order, so a
    PPC virtual control drives $\xi$ to zero while superposing the
    collision-avoidance potential $\phi_i$ (the fence).
    \item Distributed implementation: each agent computes its local term
    from neighbors $\NN_i(t)$; discuss how $\dist(p,p_0)$ (a global
    quantity) is replaced by a locally estimable surrogate (e.g.\ centroid
    estimate via consensus, as in the finite-time centroid enhancer).
    \item Prescribed-time variant: choose virtual gain
    $a(t)=\frac{\pi}{2T}\sec^2(\frac{\pi t}{2T})$ with the performance
    function \eqref{eq:rho-pt} so that $\xi(t)=\xi(0)\cos(\frac{\pi t}{2T})$
    reaches zero exactly at $t=T$ (note the gain diverges as $t\to T^-$).
\end{itemize}
